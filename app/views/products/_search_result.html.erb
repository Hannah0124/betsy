<div class="products__index_product-container item">
  <%= link_to(image_tag(product.photo_url, :alt => "product", class: 'products__index_product-img'), product_path(product.id)) %>

  <div class="products__index_product-info-container">
    <h6><%= link_to "#{product.name}", product_path(product) %></h6>
    <p>🔔 <%= product.price %> Bells</p> 
    <p> Categories: <%= product.display_categories %></p>
    <p><span class="small-stars"><%= Review.display_rating(product.average_rating) %></span>(<%= product.average_rating %>/ 5)</p> 
    <p>Sold by <strong><%= link_to "#{User.find_by(id: product.user_id).name}", user_path(product.user_id) %></strong></p>
  </div>
</div>